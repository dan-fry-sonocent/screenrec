version: '3'

tasks:
  dev:
    desc: Start the Vite development server
    cmds:
      - npm run dev

  build:
    desc: Build for production (output to dist/)
    cmds:
      - npm run build

  preview:
    desc: Preview the production build locally
    cmds:
      - npm run preview
